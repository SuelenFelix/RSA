
let p = 11;
let q = 17;
let n = (p*q);
let z = (p-1) * (q-1);
let d = 7;
let e = 23; 


function encrepitar(entrada){

  return (Math.pow(entrada, e)  % N);

}

function desencripitar(){
  
  return (Math.pow(entrada, d)  % N);

}